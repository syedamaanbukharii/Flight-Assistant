# ---------------------------------------------------------------------------
# Development & test dependencies.
# Install:  pip install -r requirements-dev.txt
# (includes everything in requirements.txt)
# ---------------------------------------------------------------------------
-r requirements.txt

pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# Linting / formatting (optional but configured in pyproject.toml)
ruff>=0.5,<1.0
